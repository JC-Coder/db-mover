# feature suggestion

1. Add support so users can upload zip of database file directly and provide target url , the app should extract and push the database to the target url.
2. show a modal or toast when the self hosted version a user is using is different from live version, mostly when things have changed it means version change / update
3. i'm thinking of adding a new feature
when user want to copy / download data from db, when they provide the source url, they should be able to enable / toggle partial copy / download
when this is enabled there will be a popup with checkmark so they can include / exclude tables and all .
what do you think ? what am i missing that can be added
how difficult is it to implement this feature?

# bugs

1. 